diff --git a/node_modules/@expo/cli/build/src/api/rest/cache/wrapFetchWithCache.js b/node_modules/@expo/cli/build/src/api/rest/cache/wrapFetchWithCache.js
index daad31c..3cb875a 100644
--- a/node_modules/@expo/cli/build/src/api/rest/cache/wrapFetchWithCache.js
+++ b/node_modules/@expo/cli/build/src/api/rest/cache/wrapFetchWithCache.js
@@ -36,6 +36,9 @@ function wrapFetchWithCache(fetch, cache) {
             if (!response.ok || !response.body) {
                 return response;
             }
+            // Keep an untouched fallback: cache.set() tees/consumes response.body,
+            // so if the cache write or read-back fails, `response` is no longer readable.
+            const fallbackResponse = response.clone();
             // Cache the response
             cachedResponse = await cache.set(cacheKey, {
                 body: response.body,
@@ -45,7 +48,7 @@ function wrapFetchWithCache(fetch, cache) {
             if (!cachedResponse) {
                 debug(`Failed to cache response for: ${url}`);
                 await cache.remove(cacheKey);
-                return response;
+                return fallbackResponse;
             }
             // Return the cached response
             return new (_undici()).Response(cachedResponse.body, cachedResponse.info);
